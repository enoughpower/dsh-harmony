# 会话总结（2026-09-05）

> 一次跨 App 开发、工具链升级、模型排障的长会话。按工作流归纳，含关键结论与踩坑，供后续会话参考。

## 元信息

| 项 | 内容 |
|---|---|
| 日期 | 2026-09-05 |
| 默认模型 | deepseek-v4-flash（deepseek-official） |
| 主要范围 | v2.0.1 发布、CLT 821 升级、DevEco CLI 接入、API 26 适配、UI 大改 |
| 相关提交 | fe8ab75 等 |

## 目录

- [1. 应用发布与装机](#1-应用发布与装机)
- [2. 打包工具链升级](#2-打包工具链升级重要)
- [3. DevEco CLI / Code 工具链接入](#3-deveco-cli--code-工具链接入)
- [4. API 26 适配](#4-api-26-适配app-升级-target-26)
- [5. App UI 大改](#5-app-ui-大改)
- [6. 遗留与下一步](#6-遗留与下一步)

---

## 1. 应用发布与装机

- 为手机（HUAWEI Pura 80 Pro）构建并安装最新 debug 包（网络 hdc：hdc tconn 192.168.5.18:34413）。
- 发布 **v2.0.1**：
  - 升 versionName/versionCode 为 2.0.1/1000006
  - 新增 release/RELEASE_NOTES.v2.0.1.md
  - CHANGELOG 加 v2.0.1 标记
  - 打 tag v2.0.1 并 push
  - sign-release.sh 产出 release 签名 HAP
  - gh release create 发布到 GitHub Release（标记 Latest）
- **结论**：GitHub Release 只放 release 签名包（发布证书，供云测/平台侧载），**不传 debug 装机包**（用户明确）。

## 2. 打包工具链升级（重要）

- 本机打包工具链换成 **Command Line Tools 26.0.0.821（Release）**：解压后替换 ~/.harmony/command-line-tools-mac26（脚本/CI 路径不变即生效），旧 621 备份后删除。
- 821 对应版本：
  - DevEco Studio 26.0.0.821
  - SDK 26.0.0.105
  - hvigor 6.26.4
  - ohpm 26.0.0.630
- 清理 ~/Downloads 旧工具链（621/Linux 全删 + 当前 821 源副本 + 621 CLT 备份），释放约 **23G**。
- 关键踩坑：**API 26 起版本号改 SemVer X.Y.Z**（不再用 X.Y.Z(N)）。build-profile.json5 的 targetSdkVersion 写 "26.0.0(26)" 会报 00308018 api version parameter is illegal，应写 "26.0.0"。
  - build-profile.json5 是 skip-worktree（含签名密钥，永不入库）。该踩坑点已写入本地 skill 记录。

## 3. DevEco CLI / Code 工具链接入

- 安装 @deveco/deveco-cli（devecocli）+ @deveco/deveco-code（deveco，AI Agent），均已可用。
- 新增工具：
  - scripts/devecocli.sh：自动指向 821 CLT + DevEco Studio 的统一入口
  - code-linter.json5：支撑 devecocli check lint
  - 本地离线文档 docs search/read（API 9-26 版本完整）
  - --mcp 注入 deveco-mcp
- 新增真机 UI 冒烟脚本 scripts/ui-smoke.sh（devecocli ui layout 断言 + 截图 + 日志；真机验证通过）。
- harmony-next skill 保留（专用工具/私有 DevEco 接口仍有用；文档查询改走 devecocli docs）。

## 4. API 26 适配（App 升级 target 26）

- devecocli check compat 扫描 6.1.1(24) -> 26.0.0(26)：共 871 项
  - 定义变更 613 项（无影响）
  - UX 变更 253 项
  - **接口行为变更 5 项**：shadow 模糊半径 + clearAllCookiesSync 的 ArkWeb Cookie 目录变更（全部生效，4 处调用受影响）
- 工程 targetSdkVersion 升到 "26.0.0"（SemVer 写法）。
- 821 构建装机后，dsh-pocket 会话链路真机验证正常。

## 5. App UI 大改

对应提交：fe8ab75 及后续 push。

- **底部双 Tab（首页 + 会话列表）**：使用官方 UI Design Kit 的 **HdsTabs** 悬浮胶囊 TabBar。
  - BottomTabBarStyle + .barFloatingStyle() + barOverlap(true) + hdsMaterial IMMERSIVE
  - 图标用 App 自有 SVG：tab_home.svg / tab_list.svg
  - 颜色：未选中 text_primary（浅黑/深白）、选中主题蓝（labelStyle/iconStyle）
- **pages/TasksPage -> 可嵌入 SessionListTab 组件**（从 main_pages 移除）。
- **余额卡**：
  - ¥金额 CNY 排版
  - 低于推送阈值（KEY_PUSH_BALANCE_THRESHOLD，默认 5）显示红色 #FF3B30
  - 刷新动画：数字滚动（ease-out）+ 缩放脉冲（animateBalance + balancePulse）
- **会话列表**：
  - 按工作区（cwd）分组展示（组头 = 目录名 + 会话数）
  - 内容延伸到底部、**只避让手势条**（悬浮 TabBar 允许覆盖）
- 首页删除「会话列表」入口卡；两张卡内边距统一为 16。
- **修复下拉刷新状态不更新**：ArkUI ForEach 同 key 复用不重建 -> 加入 refreshSeq 作为 ForEach key，每次取数后强制重建。
- **修复归档/空白会话过滤回归**（08-31 协议重写时丢失）：
  - 恢复 workspace.list 的 archivedSessionIds 过滤
  - 恢复 0 轮 0 步空白会话过滤（queryArchivedIds）
  - 注意：workspace.list 在本版 dsh-pocket 返回 not found，会静默返回 []。

## 6. 遗留与下一步

- [ ] （可选）ArkWeb Cookie 目录变更（Chromium 144）对 dsh-pocket 会话的专项回归。
- [ ] 本轮新 UI（双 Tab / 分组 / 余额卡）如无问题即定稿；有需要再微调。

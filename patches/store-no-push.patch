diff --git a/entry/src/main/ets/common/store/PushToken.ets b/entry/src/main/ets/common/store/PushToken.ets
deleted file mode 100644
index 4830c6b..0000000
--- a/entry/src/main/ets/common/store/PushToken.ets
+++ /dev/null
@@ -1,116 +0,0 @@
-import { pushService } from '@kit.PushKit';
-import { common } from '@kit.AbilityKit';
-import { http } from '@kit.NetworkKit';
-import { BusinessError } from '@kit.BasicServicesKit';
-import { hilog } from '@kit.PerformanceAnalysisKit';
-import { fileIo } from '@kit.CoreFileKit';
-import { util } from '@kit.ArkTS';
-import { ConnectionStore } from './ConnectionStore';
-
-const DOMAIN = 0x0000;
-const TAG = 'DSHHarmony';
-
-/** push.token 请求体 */
-interface PushTokenRpc {
-  'type': string;
-  'rpcId': string;
-  'method': string;
-  'payload': PushTokenPayload;
-}
-interface PushTokenPayload {
-  'token': string;
-}
-
-/**
- * Push Kit 推送 Token：
- * ① 启动时用 pushService.getToken() 获取设备推送令牌
- * ② 上报给电脑端推送器（独立 Node 服务，见 tools/push-notify/）
- * 前置：应用需在 AGC 开通推送服务（否则 getToken 报 1000900012）
- */
-export class PushToken {
-  /** 从 AGC 开通推送后调用，取得 token */
-  static async getToken(): Promise<string> {
-    return await pushService.getToken();
-  }
-
-  /** 写调试日志（getToken 诊断用） */
-  private static logDebug(context: common.Context, msg: string): void {
-    try {
-      const path = context.filesDir + '/pocket-debug.log';
-      let old = '';
-      if (fileIo.accessSync(path)) {
-        const rf = fileIo.openSync(path, fileIo.OpenMode.READ_WRITE);
-        const buf = new ArrayBuffer(16384);
-        const n = fileIo.readSync(rf.fd, buf);
-        old = n > 0 ? util.TextDecoder.create('utf-8').decodeToString(new Uint8Array(buf, 0, n)) : '';
-        fileIo.closeSync(rf);
-      }
-      old = old + new Date().toString() + ' PT: ' + msg + '\n';
-      if (old.length > 65536) {
-        old = old.slice(old.length - 65536);
-      }
-      const wf = fileIo.openSync(path, fileIo.OpenMode.READ_WRITE | fileIo.OpenMode.CREATE);
-      fileIo.writeSync(wf.fd, old);
-      fileIo.closeSync(wf);
-    } catch (e) {
-    }
-  }
-
-  /** 启动时：取 token → 上报给电脑端推送器 */
-  static async ensureAndReport(context: common.Context): Promise<void> {
-    try {
-      const token = await PushToken.getToken();
-      PushToken.logDebug(context, 'getToken ok len=' + token.length);
-      hilog.info(DOMAIN, TAG, 'push token obtained len=%{public}d', token.length);
-      await PushToken.report(context, token);
-    } catch (e) {
-      const be = e as BusinessError;
-      // 1000900012 = 推送未激活（未在 AGC 开通）；1000900010 = 应用身份非法（指纹/应用配置）
-      PushToken.logDebug(context, 'getToken FAILED code=' + be.code + ' msg=' + be.message.slice(0, 120));
-      hilog.warn(DOMAIN, TAG, 'getToken failed code=%{public}d msg=%{public}s', be.code, be.message);
-    }
-  }
-
-  /** 上报 token：找最近连接，POST /api/push.token 给电脑端推送器 */
-  private static async report(context: common.Context, token: string): Promise<void> {
-    const list = await new ConnectionStore().loadAll(context);
-    if (list.length === 0) {
-      return;
-    }
-    const base = list[0].url.replace(/\/+$/, '');
-    // 上报给电脑端推送服务（独立进程 :3082；从连接地址取主机、换端口）
-    let host: string = '';
-    const protoEnd = base.indexOf('://');
-    if (protoEnd >= 0) {
-      let rest = base.substring(protoEnd + 3);
-      const colon = rest.indexOf(':');
-      host = colon >= 0 ? rest.substring(0, colon) : rest;
-    }
-    const reportBase = host.length > 0 ? 'http://' + host + ':3082' : base;
-    const rpc: PushTokenRpc = {
-      'type': 'client-request',
-      'rpcId': 'push-token-' + Date.now(),
-      'method': 'push.token',
-      'payload': { 'token': token }
-    };
-    const body = JSON.stringify(rpc);
-    const req = http.createHttp();
-    try {
-      const headers: Record<string, string> = { 'Content-Type': 'application/json' };
-      const options: http.HttpRequestOptions = {
-        'method': http.RequestMethod.POST,
-        'header': headers,
-        'extraData': body,
-        'connectTimeout': 5000,
-        'readTimeout': 5000
-      };
-      const resp: http.HttpResponse = await req.request(reportBase + '/api/push.token', options);
-      hilog.info(DOMAIN, TAG, 'push.token report code=%{public}d', resp.responseCode);
-    } catch (e) {
-      const be = e as BusinessError;
-      hilog.warn(DOMAIN, TAG, 'push.token report failed %{public}s', be.message);
-    } finally {
-      req.destroy();
-    }
-  }
-}
diff --git a/entry/src/main/ets/entryability/EntryAbility.ets b/entry/src/main/ets/entryability/EntryAbility.ets
index d968c5b..b3754e4 100644
--- a/entry/src/main/ets/entryability/EntryAbility.ets
+++ b/entry/src/main/ets/entryability/EntryAbility.ets
@@ -1,7 +1,6 @@
 import { AbilityConstant, UIAbility, Want, Configuration } from '@kit.AbilityKit';
 import { hilog } from '@kit.PerformanceAnalysisKit';
 import { window } from '@kit.ArkUI';
-import { PushToken } from '../common/store/PushToken';
 import { WindowBar } from '../common/utils/WindowBar';
 
 const DOMAIN = 0x0000;
@@ -18,9 +17,6 @@ export default class EntryAbility extends UIAbility {
   onCreate(want: Want, launchParam: AbilityConstant.LaunchParam): void {
     hilog.info(DOMAIN, TAG, 'EntryAbility onCreate. reason=%{public}d', launchParam.launchReason);
     this.consumeCardUrl(want);
-    // Push Kit：获取设备推送令牌并上报电脑端（未在 AGC 开通时静默，报 1000900012）
-    PushToken.ensureAndReport(this.context).catch(() => {
-    });
   }
 
   onNewWant(want: Want): void {
diff --git a/entry/src/main/ets/pages/Index.ets b/entry/src/main/ets/pages/Index.ets
index bf7684a..e1b9063 100644
--- a/entry/src/main/ets/pages/Index.ets
+++ b/entry/src/main/ets/pages/Index.ets
@@ -13,10 +13,8 @@ import { UrlInputDialog } from '../components/UrlInputDialog';
 import { CardSync } from '../common/store/CardSync';
 import { AppPrefs } from '../common/store/AppPrefs';
 import { WindowBar } from '../common/utils/WindowBar';
-import { PushToken } from '../common/store/PushToken';
 import { PocketApi, LatestSessionInfo, SessionQueryResult } from '../common/utils/PocketApi';
 import { http } from '@kit.NetworkKit';
-import { notificationManager } from '@kit.NotificationKit';
 
 const DOMAIN = 0x0000;
 const TAG = 'DSHHarmony';
@@ -34,11 +32,9 @@ struct Index {
   @State latestSession: LatestSessionInfo | null = null;
   @State sessionLoading: boolean = false;
   @State sessionError: string = '';
-  @State pushOnline: boolean = false;
-  @State pushChecking: boolean = false;
+
   /** 状态栏高度（沉浸式时内容顶部留白），onPageShow 实测校正 */
   @State statusBarH: number = 28;
-  private pushTimer: number = -1;
   private sessionTimer: number = -1;
   private store: ConnectionStore = new ConnectionStore();
   /** 会话内是否已自动连接过（防止 WebShell 返回时重复拉/自动拉回） */
@@ -66,31 +62,7 @@ struct Index {
     });
   }
 
-  /** 新装首启:请求通知权限(推送通知展示前提;重复调用无弹窗副作用) */
-  private notifyRequested: boolean = false;
-
-  private async ensureNotifyPermission(): Promise<void> {
-    if (this.notifyRequested) {
-      return;
-    }
-    this.notifyRequested = true;
-    try {
-      const enabled = await notificationManager.isNotificationEnabled();
-      if (enabled) {
-        hilog.info(DOMAIN, TAG, 'notification already enabled');
-        return;
-      }
-      const ctx = this.getUIContext().getHostContext() as common.UIAbilityContext;
-      await notificationManager.requestEnableNotification(ctx);
-      hilog.info(DOMAIN, TAG, 'notification permission requested');
-    } catch (e) {
-      hilog.warn(DOMAIN, TAG, 'request notification permission failed: %{public}s', (e as Error).message);
-    }
-  }
-
   onPageShow(): void {
-    // 新装首启请求通知权限(推送链路前置)
-    this.ensureNotifyPermission();
     this.reloadHistory();
     // 沉浸式系统栏兜底：幂等重设（冷启动/时序问题/去其他页面后回首页都保证正确），并实测状态栏高度
     const barCtx = this.getUIContext().getHostContext() as common.UIAbilityContext;
@@ -108,13 +80,6 @@ struct Index {
     });
     // 最新会话卡片（首页展示）
     this.refreshSession();
-    // 电脑端推送服务在线状态（立即 + 15s 轮询）
-    this.checkPushStatus();
-    if (this.pushTimer < 0) {
-      this.pushTimer = setInterval(() => {
-        this.checkPushStatus();
-      }, 15000);
-    }
     if (this.sessionTimer < 0) {
       this.sessionTimer = setInterval(() => {
         this.refreshSession();
@@ -132,10 +97,6 @@ struct Index {
   }
 
   onPageHide(): void {
-    if (this.pushTimer >= 0) {
-      clearInterval(this.pushTimer);
-      this.pushTimer = -1;
-    }
     if (this.sessionTimer >= 0) {
       clearInterval(this.sessionTimer);
       this.sessionTimer = -1;
@@ -180,61 +141,12 @@ struct Index {
       CardSync.syncAll(ctx).catch(() => {
         hilog.warn(DOMAIN, TAG, 'card sync failed');
       });
-      // 连接建立后立即上报推送 token(重装/首连场景不等冷启动)
-      PushToken.ensureAndReport(ctx).catch(() => {
-      });
     }).catch((e: BusinessError) => {
       hilog.warn(DOMAIN, TAG, 'save connection failed: %{public}s', e.message);
     });
     router.pushUrl({ url: 'pages/WebShell', params: { 'url': url } });
   }
 
-  /** 检查电脑端推送服务在线状态（GET host:3082/api/status，3s 超时） */
-  private checkPushStatus(): void {
-    if (this.pushChecking) {
-      return;
-    }
-    this.pushChecking = true;
-    const ctx = this.getUIContext().getHostContext() as common.UIAbilityContext;
-    this.store.loadAll(ctx).then((list: ConnectionProfile[]) => {
-      if (list.length === 0) {
-        this.pushOnline = false;
-        this.pushChecking = false;
-        return;
-      }
-      const base = list[0].url.replace(/\/+$/, '');
-      let host: string = '';
-      const protoEnd = base.indexOf('://');
-      if (protoEnd >= 0) {
-        let rest = base.substring(protoEnd + 3);
-        const colon = rest.indexOf(':');
-        host = colon >= 0 ? rest.substring(0, colon) : rest;
-      }
-      if (host.length === 0) {
-        this.pushOnline = false;
-        this.pushChecking = false;
-        return;
-      }
-      const req = http.createHttp();
-      const options: http.HttpRequestOptions = {
-        'method': http.RequestMethod.GET,
-        'connectTimeout': 3000,
-        'readTimeout': 3000
-      };
-      req.request('http://' + host + ':3082/api/status', options).then((resp: http.HttpResponse) => {
-        this.pushOnline = resp.responseCode === 200;
-      }).catch(() => {
-        this.pushOnline = false;
-      }).finally(() => {
-        req.destroy();
-        this.pushChecking = false;
-      });
-    }).catch(() => {
-      this.pushOnline = false;
-      this.pushChecking = false;
-    });
-  }
-
   /** 拉取最新会话（首页展示；LAN PIN 登录 + session.list） */
   /** 追加调试日志到沙箱文件（pocket-debug.log，hdc 可读） */
   private logDebug(context: common.Context, msg: string): void {
@@ -388,23 +300,6 @@ struct Index {
     .width('100%')
   }
 
-  /** 电脑端推送服务在线状态 */
-  @Builder
-  PushStatusRow() {
-    Row({ space: 8 }) {
-      Circle()
-        .width(8)
-        .height(8)
-        .fill(this.pushOnline ? '#34C759' : '#FF3B30')
-      Text(this.pushOnline ? '推送服务在线' : '推送服务离线')
-        .fontSize(12)
-        .fontColor($r('app.color.text_secondary'))
-      Blank()
-    }
-    .width('100%')
-    .padding({ left: 4, right: 4 })
-  }
-
   @Builder
   LatestSessionCard() {
     Column({ space: 10 }) {
@@ -524,8 +419,7 @@ struct Index {
     Column({ space: 16 }) {
       this.Header()
 
-      this.PushStatusRow()
-
+  
       this.LatestSessionCard()
 
       if (this.connections.length > 0) {
diff --git a/entry/src/main/ets/pages/SettingsPage.ets b/entry/src/main/ets/pages/SettingsPage.ets
index 41d3676..1316339 100644
--- a/entry/src/main/ets/pages/SettingsPage.ets
+++ b/entry/src/main/ets/pages/SettingsPage.ets
@@ -339,7 +339,7 @@ struct SettingsPage {
             .fontSize(15)
             .fontColor($r('app.color.text_primary'))
           Text('随身连接你电脑上的 AI 工作区。扫码或输入电脑端地址，手机即可查看会话、' +
-            '管理任务并接收完成提醒。')
+            '管理任务。')
             .fontSize(13)
             .fontColor($r('app.color.text_secondary'))
             .textAlign(TextAlign.Start)
